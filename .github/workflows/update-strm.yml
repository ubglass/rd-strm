name: Build Real-Debrid STRM Library (FINAL MOVIE FIX)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"  # every hour

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Prepare Folders
        run: |
          set +e
          rm -rf strm || true
          mkdir -p strm/Movies
          mkdir -p strm/TV_Shows

      ####################################################################
      # ROOT INDEX
      ####################################################################
      - name: Create Root Index
        run: |
          echo "<h1>RD STRM Library</h1>" > strm/index.html
          echo "<ul>" >> strm/index.html
          echo "<li><a href='Movies/'>Movies</a></li>" >> strm/index.html
          echo "<li><a href='TV_Shows/'>TV Shows</a></li>" >> strm/index.html
          echo "</ul>" >> strm/index.html

      ####################################################################
      # FETCH RD TORRENTS
      ####################################################################
      - name: Fetch RD Torrents
        run: |
          curl -s -H "Authorization: Bearer ${{ secrets.RDTOKEN }}" \
            "https://api.real-debrid.com/rest/1.0/torrents" \
            > torrents.json

      ####################################################################
      # BUILD STRM FILES (MOVIES + TV SHOWS)
      ####################################################################
      - name: Build STRM Files
        run: |
          set +e
          CUTOFF=$(date -d "15 days ago" +%s)

          jq -c '.[]?' torrents.json | while read TORRENT; do
            ID=$(echo "$TORRENT" | jq -r '.id')
            NAME=$(echo "$TORRENT" | jq -r '.filename')
            ADDED=$(echo "$TORRENT" | jq -r '.added')
            [ -z "$ID" ] && continue

            ADDED_EPOCH=$(date -d "$ADDED" +%s 2>/dev/null || echo 0)
            [ "$ADDED_EPOCH" -lt "$CUTOFF" ] && continue

            echo "Processing: $NAME"

            curl -s -H "Authorization: Bearer ${{ secrets.RDTOKEN }}" \
              "https://api.real-debrid.com/rest/1.0/torrents/info/$ID" \
              > detail.json

            FILECOUNT=$(jq '.files | length' detail.json 2>/dev/null || echo 0)
            [ "$FILECOUNT" -lt 1 ] && continue

            for i in $(seq 0 $(($FILECOUNT - 1))); do

              FILE=$(jq -r ".files[$i].path" detail.json)
              LINK=$(jq -r ".links[$i]?" detail.json)

              BASENAME=$(basename "$FILE")
              EXT="${BASENAME##*.}"
              [[ ! "$EXT" =~ ^(mkv|mp4|avi|mov|m4v|webm)$ ]] && continue
              [ "$LINK" = "null" ] && continue

              echo "→ Unrestricting $BASENAME"

              UNR=$(curl -s -X POST \
                -H "Authorization: Bearer ${{ secrets.RDTOKEN }}" \
                -d "link=$LINK" \
                "https://api.real-debrid.com/rest/1.0/unrestrict/link")

              DIRECT=$(echo "$UNR" | jq -r '.download')
              [ "$DIRECT" = "null" ] && continue

              ################################################################
              # TV SHOW PARSING
              ################################################################
              if [[ "$BASENAME" =~ [Ss][0-9][0-9][Ee][0-9][0-9] ]]; then

                SHOW=$(echo "$BASENAME" | sed -E 's/\.[Ss][0-9]+.*//;s/[._]/ /g')

                RAWSEASON=$(echo "$BASENAME" | grep -oE '[Ss][0-9]+' | tr -d 'Ss')
                RAWEPI=$(echo "$BASENAME" | grep -oE '[Ee][0-9]+' | tr -d 'Ee')

                SNUM=$((10#$RAWSEASON))
                ENUM=$((10#$RAWEPI))

                SEASONPATH="strm/TV_Shows/$SHOW/Season $SNUM"
                mkdir -p "$SEASONPATH"

                OUT="$SEASONPATH/$SHOW S$(printf '%02d' $SNUM)E$(printf '%02d' $ENUM).strm"
                echo "$DIRECT" > "$OUT"

                continue
              fi

              ################################################################
              # MOVIE PARSING (FINAL, FIXED)
              ################################################################
              TITLE=$(echo "$BASENAME" | sed -E 's/[._]/ /g')

              YEAR=$(echo "$BASENAME" | grep -oE '\b(19[0-9]{2}|20[0-3][0-9])\b' | head -n 1)
              RES=$(echo "$BASENAME" | grep -oE '2160p|1080p|720p' | head -n 1)

              CLEAN_TITLE=$(echo "$TITLE" | sed -E 's/\b(19[0-9]{2}|20[0-3][0-9])\b.*//')

              MOVIEDIR="strm/Movies/$CLEAN_TITLE ($YEAR)"
              mkdir -p "$MOVIEDIR"

              FILENAME="$CLEAN_TITLE ($YEAR)"
              [ -n "$RES" ] && FILENAME="$FILENAME $RES"
              FILENAME="$FILENAME.strm"

              OUT="$MOVIEDIR/$FILENAME"
              echo "$DIRECT" > "$OUT"

            done
          done

      ####################################################################
      # GENERATE filelist.xml FOR MOVIES & TV SHOWS
      ####################################################################
      - name: Generate XML
        run: |
          escape_xml() {
            echo "$1" \
              | sed 's/&/&amp;/g' \
              | sed 's/</&lt;/g' \
              | sed 's/>/&gt;/g' \
              | sed 's/\"/&quot;/g' \
              | sed "s/'/&apos;/g"
          }

          generate_xml() {
            DIR="$1"
            OUT="$DIR/filelist.xml"
            echo "<files>" > "$OUT"

            find "$DIR" -maxdepth 1 -type f -name "*.strm" | while read F; do
              BASE=$(basename "$F")
              ESCAPED=$(escape_xml "$BASE")
              echo "  <file>$ESCAPED</file>" >> "$OUT"
            done

            echo "</files>" >> "$OUT"
          }

          for movie in strm/Movies/*; do
            [ -d "$movie" ] || continue
            generate_xml "$movie"
          done

          for show in strm/TV_Shows/*; do
            [ -d "$show" ] || continue
            for season in "$show"/Season*; do
              [ -d "$season" ] || continue
              generate_xml "$season"
            done
          done

      ####################################################################
      # BUILD HTML INDEXES (BROWSER/KODI DIRECTORY SUPPORT)
      ####################################################################
      - name: Build HTML Indexes
        run: |
          build_index() {
            DIR="$1"
            HTML="$DIR/index.html"

            echo "<h2>Listing: $(basename "$DIR")</h2><ul>" > "$HTML"

            for item in "$DIR"/*; do
              NAME=$(basename "$item")
              if [ -d "$item" ]; then
                echo "<li><a href='$NAME/'>$NAME/</a></li>" >> "$HTML"
              elif [[ "$NAME" == *.strm ]]; then
                echo "<li><a href='$NAME'>$NAME</a></li>" >> "$HTML"
              fi
            done

            echo "</ul>" >> "$HTML"
          }

          build_index "strm/Movies"
          for movie in strm/Movies/*; do
            [ -d "$movie" ] || continue
            build_index "$movie"
          done

          build_index "strm/TV_Shows"
          for show in strm/TV_Shows/*; do
            [ -d "$show" ] || continue
            build_index "$show"

            for season in "$show"/Season*; do
              [ -d "$season" ] || continue
              build_index "$season"
            done
          done

      ####################################################################
      # DEPLOY TO GITHUB PAGES
      ####################################################################
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: strm

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - uses: actions/deploy-pages@v4
